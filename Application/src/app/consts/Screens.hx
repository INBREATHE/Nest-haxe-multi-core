package app.consts;
class Screens {

    static private var BASE(default, never):String = "screen_";

    static public var MAIN(default, never):String = BASE + "main";

    public function new() {}
}
